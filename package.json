{
  "name": "turborepo-shadcn-ui",
  "version": "1.2.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "ui:add": "pnpm --filter @schedulr/ui ui:add"
  },
  "devDependencies": {
    "@schedulr/eslint-config": "workspace:*",
    "@schedulr/typescript-config": "workspace:*",
    "prettier": "^3.3.2",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}